# Apollo.io API Key
APOLLO_API_KEY=your_apollo_api_key_here

# Gmail credentials (will be created via OAuth)
# No need to set these manually - they'll be generated when you run the app

# Optional: OpenAI API Key for AI-powered email personalization (future feature)
OPENAI_API_KEY=your_openai_api_key_here
